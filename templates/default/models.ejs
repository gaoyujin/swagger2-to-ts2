/**
 * <%=data.summary %>
 */
export type <%= data.title %> = { <% for (let i = 0; i < keys.length;
i++) { %> 
  /* <%- descs[keys[i]].description %> */
  <%= keys[i] %>: <%- descs[keys[i]].type %>;  <% } %> 
};
